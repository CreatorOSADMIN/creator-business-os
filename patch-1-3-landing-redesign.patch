diff --git a/src/app/globals.css b/src/app/globals.css
index bdb6a12..ee2634f 100644
--- a/src/app/globals.css
+++ b/src/app/globals.css
@@ -1,18 +1,18 @@
 @import "tailwindcss";
 
 :root {
-  --background: #f7f7f4;
-  --foreground: #14151a;
-  --ink-muted: #52545e;
-  --surface: #ffffff;
-  --surface-dark: #14151a;
-  --border-subtle: #e4e3dd;
-  --accent: #0f6f5c;
-  --accent-soft: #e4f0ec;
-  --accent-strong: #0a4f42;
+  --background: #0a0a0b;
+  --foreground: #f5f5f2;
+  --ink-muted: #94949c;
+  --surface: #141416;
+  --surface-raised: #1c1c1f;
+  --surface-dark: #000000;
+  --border-subtle: rgba(255, 255, 255, 0.08);
+  --accent: #16a085;
+  --accent-soft: rgba(22, 160, 133, 0.14);
+  --accent-strong: #0f6f5c;
   --highlight: #e8963a;
-  --danger: #b3402f;
-  --font-display: ui-serif, Georgia, "Times New Roman", serif;
+  --danger: #e5484d;
   --font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI", Inter, Roboto, Helvetica, Arial, sans-serif;
   --font-mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
 }
@@ -41,10 +41,6 @@ body {
   color: var(--foreground);
 }
 
-.font-display {
-  font-family: var(--font-display);
-}
-
 .font-mono-label {
   font-family: var(--font-mono);
   letter-spacing: 0.08em;
diff --git a/src/app/page.tsx b/src/app/page.tsx
index 6da5961..21f8f08 100644
--- a/src/app/page.tsx
+++ b/src/app/page.tsx
@@ -20,8 +20,7 @@ export default async function HomePage() {
         <Hero />
         <Problem />
         <Solution />
-        <DevelopmentStatus />
-        <EarlyAccessBand progress={progress} goal={goal} />
+        <StatusAndEarlyAccess progress={progress} goal={goal} />
       </main>
       <SiteFooter />
     </>
@@ -36,7 +35,7 @@ function Hero() {
           <p className="font-mono-label text-xs uppercase text-[var(--accent)]">
             Now building — early access open
           </p>
-          <h1 className="mt-5 font-display text-5xl leading-[1.05] tracking-tight sm:text-6xl">
+          <h1 className="mt-5 font-sans font-semibold text-5xl leading-[1.05] tracking-tight sm:text-6xl">
             Build smarter.
             <br />
             Grow with data.
@@ -50,7 +49,7 @@ function Hero() {
               href="/early-access"
               event="early_access_click"
               eventProps={{ location: "hero" }}
-              className="rounded-full bg-[var(--accent)] px-6 py-3 text-sm font-medium text-white transition hover:bg-[var(--accent-strong)]"
+              className="rounded-full bg-white px-6 py-3 text-sm font-medium text-[var(--background)] transition hover:bg-white/90"
             >
               Join Early Access
             </TrackedLink>
@@ -76,9 +75,14 @@ function SignalCard() {
   ];
   return (
     <div className="rounded-2xl border border-[var(--border-subtle)] bg-[var(--surface)] p-6 shadow-[0_1px_0_0_rgba(0,0,0,0.03)]">
-      <p className="font-mono-label text-[11px] uppercase text-[var(--ink-muted)]">
-        A preview of what we&apos;re building
-      </p>
+      <div className="flex items-center justify-between gap-3">
+        <p className="font-mono-label text-[11px] uppercase text-[var(--ink-muted)]">
+          A preview of what we&apos;re building
+        </p>
+        <span className="rounded-full bg-[var(--accent-soft)] px-2.5 py-1 font-mono-label text-[10px] uppercase text-[var(--accent)]">
+          In development
+        </span>
+      </div>
       <div className="mt-4 space-y-4">
         {rows.map((row) => (
           <div key={row.label}>
@@ -121,7 +125,7 @@ function Problem() {
     <section id="problem" className="border-b border-[var(--border-subtle)] bg-[var(--surface)]">
       <div className="mx-auto max-w-6xl px-6 py-20">
         <p className="font-mono-label text-xs uppercase text-[var(--accent)]">The problem</p>
-        <h2 className="mt-3 max-w-xl font-display text-3xl tracking-tight sm:text-4xl">
+        <h2 className="mt-3 max-w-xl font-sans font-semibold text-3xl tracking-tight sm:text-4xl">
           Creating today means managing chaos, not just content.
         </h2>
         <div className="mt-12 grid gap-8 sm:grid-cols-3">
@@ -148,7 +152,7 @@ function Solution() {
     <section className="border-b border-[var(--border-subtle)]">
       <div className="mx-auto max-w-6xl px-6 py-20">
         <p className="font-mono-label text-xs uppercase text-[var(--accent)]">What we&apos;re building</p>
-        <h2 className="mt-3 max-w-xl font-display text-3xl tracking-tight sm:text-4xl">
+        <h2 className="mt-3 max-w-xl font-sans font-semibold text-3xl tracking-tight sm:text-4xl">
           One system, built with creators from day one.
         </h2>
         <p className="mt-4 max-w-2xl text-[var(--ink-muted)]">
@@ -170,60 +174,40 @@ function Solution() {
   );
 }
 
-function DevelopmentStatus() {
+function StatusAndEarlyAccess({ progress, goal }: { progress: number; goal: number }) {
   const stages = [
     { label: "Foundation & early access", state: "In progress" },
     { label: "Unified analytics", state: "Planned" },
     { label: "Insight & recommendations", state: "Planned" },
   ];
   return (
-    <section className="border-b border-[var(--border-subtle)] bg-[var(--surface)]">
-      <div className="mx-auto max-w-6xl px-6 py-20">
-        <p className="font-mono-label text-xs uppercase text-[var(--accent)]">Project status</p>
-        <h2 className="mt-3 max-w-xl font-display text-3xl tracking-tight sm:text-4xl">
-          CreatorOS is currently in development.
+    <section className="bg-[var(--surface-dark)] text-white">
+      <div className="mx-auto max-w-6xl px-6 py-28">
+        <p className="font-mono-label text-xs uppercase text-white/50">Project status</p>
+        <h2 className="mt-3 max-w-xl font-sans font-semibold text-3xl tracking-tight sm:text-4xl">
+          CreatorOS is currently in development — and inviting early creators in.
         </h2>
-        <p className="mt-4 max-w-2xl text-[var(--ink-muted)]">
+        <p className="mt-4 max-w-2xl text-white/70">
           We&apos;re building the first version, not maintaining a finished product. Early
-          adopters help define what CreatorOS becomes — the project is alive and changes as we
-          learn from the creators who join us.
+          adopters help define what CreatorOS becomes.
         </p>
-        <div className="mt-10 flex flex-wrap gap-3">
+        <div className="mt-8 flex flex-wrap gap-3">
           {stages.map((stage) => (
             <div
               key={stage.label}
-              className="flex items-center gap-2 rounded-full border border-[var(--border-subtle)] px-4 py-2 text-sm"
+              className="flex items-center gap-2 rounded-full border border-white/10 px-4 py-2 text-sm"
             >
               <span className="font-medium">{stage.label}</span>
-              <span className="rounded-full bg-[var(--accent-soft)] px-2 py-0.5 text-[11px] font-medium text-[var(--accent)]">
+              <span className="rounded-full bg-white/10 px-2 py-0.5 text-[11px] font-medium text-white/80">
                 {stage.state}
               </span>
             </div>
           ))}
         </div>
-        <Link
-          href="/updates"
-          className="mt-6 inline-block text-sm font-medium text-[var(--foreground)] underline decoration-[var(--border-subtle)] underline-offset-4 hover:decoration-[var(--accent)]"
-        >
-          See the full roadmap →
-        </Link>
-      </div>
-    </section>
-  );
-}
 
-function EarlyAccessBand({ progress, goal }: { progress: number; goal: number }) {
-  return (
-    <section className="bg-[var(--surface-dark)] text-white">
-      <div className="mx-auto max-w-6xl px-6 py-20">
-        <div className="grid gap-10 lg:grid-cols-[1fr_auto] lg:items-center">
+        <div className="mt-14 grid gap-10 border-t border-white/10 pt-14 lg:grid-cols-[1fr_auto] lg:items-center">
           <div>
-            <p className="font-mono-label text-xs uppercase text-white/50">Early access</p>
-            <h2 className="mt-3 max-w-xl font-display text-3xl tracking-tight sm:text-4xl">
-              CreatorOS is currently inviting a limited number of creators to join its early
-              access program.
-            </h2>
-            <ul className="mt-6 space-y-2 text-sm text-white/70">
+            <ul className="space-y-2 text-sm text-white/70">
               <li>— Early access to the platform as it&apos;s built</li>
               <li>— A direct line to influence what we build next</li>
               <li>
@@ -237,11 +221,18 @@ function EarlyAccessBand({ progress, goal }: { progress: number; goal: number })
             href="/early-access"
             event="early_access_click"
             eventProps={{ location: "band" }}
-            className="inline-flex items-center justify-center rounded-full bg-white px-7 py-3.5 text-sm font-medium text-[var(--surface-dark)] transition hover:bg-white/90"
+            className="inline-flex items-center justify-center rounded-full bg-white px-7 py-3.5 text-sm font-medium text-[var(--background)] transition hover:bg-white/90"
           >
             Join the Early Access Program
           </TrackedLink>
         </div>
+
+        <Link
+          href="/updates"
+          className="mt-6 inline-block text-sm font-medium text-white/70 underline decoration-white/20 underline-offset-4 hover:text-white"
+        >
+          See the full roadmap →
+        </Link>
       </div>
     </section>
   );
diff --git a/src/components/site-header.tsx b/src/components/site-header.tsx
index 30c701d..bf09530 100644
--- a/src/components/site-header.tsx
+++ b/src/components/site-header.tsx
@@ -17,7 +17,7 @@ export function SiteHeader() {
     <header className="sticky top-0 z-40 border-b border-[var(--border-subtle)] bg-[var(--background)]/90 backdrop-blur">
       <div className="mx-auto flex max-w-6xl items-center justify-between gap-3 px-4 py-4 sm:px-6">
         <Link href="/" className="flex items-center gap-2" onClick={() => setOpen(false)}>
-          <span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-sm bg-[var(--foreground)] font-mono-label text-[11px] font-semibold text-white">
+          <span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-sm bg-white font-mono-label text-[11px] font-semibold text-[var(--background)]">
             OS
           </span>
           <span className="font-display text-lg font-medium tracking-tight">CreatorOS</span>
@@ -32,7 +32,7 @@ export function SiteHeader() {
         <div className="flex items-center gap-2 sm:gap-3">
           <Link
             href="/early-access"
-            className="hidden rounded-full bg-[var(--accent)] px-4 py-2 text-sm font-medium text-white transition hover:bg-[var(--accent-strong)] sm:inline-flex"
+            className="hidden rounded-full bg-white px-4 py-2 text-sm font-medium text-[var(--background)] transition hover:bg-white/90 sm:inline-flex"
           >
             Join Early Access
           </Link>
@@ -78,7 +78,7 @@ export function SiteHeader() {
           <Link
             href="/early-access"
             onClick={() => setOpen(false)}
-            className="mt-3 block rounded-full bg-[var(--accent)] px-4 py-2.5 text-center text-sm font-medium text-white transition hover:bg-[var(--accent-strong)]"
+            className="mt-3 block rounded-full bg-white px-4 py-2.5 text-center text-sm font-medium text-[var(--background)] transition hover:bg-white/90"
           >
             Join Early Access
           </Link>
